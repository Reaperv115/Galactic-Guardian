#pragma once
#include <iostream>
#include "raylib.h"
#include "rlgl.h"
#include "raymath.h"
#include <numbers>
#include <cmath>

#define MAX(a, b) ((a)>(b)? (a) : (b))
#define MIN(a, b) ((a)<(b)? (a) : (b))